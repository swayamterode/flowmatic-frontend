# Base URL of the Flowmatic Spring Boot backend (no trailing slash, no context
# path). Server-only on purpose: the browser talks to Next.js Route Handlers,
# never to Spring, so this must never get a NEXT_PUBLIC_ prefix.
SPRING_API_URL=http://localhost:8080
